name: Build VLC-iOS Release IPA

on:
  workflow_dispatch:

jobs:
  build-release-ipa:
    runs-on: macos-26
    env:
      LANG: en_US.UTF-8
      LC_ALL: en_US.UTF-8

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Xcode 26.5
        run: |
          sudo xcode-select -s /Applications/Xcode_26.5.app/Contents/Developer

      - name: Show tool versions
        run: |
          xcodebuild -version
          ruby --version
          pod --version

      - name: Install CocoaPods dependencies
        run: pod update

      - name: Lint (SwiftLint)
        run: ./Pods/SwiftLint/swiftlint --config Buildsystem/swiftlint.yml

      - name: Archive VLC-iOS (Release, unsigned)
        run: |
          set -euo pipefail
          xcodebuild \
            -workspace "VLC.xcworkspace" \
            -scheme "VLC-iOS" \
            -configuration "Release" \
            -destination "generic/platform=iOS" \
            -archivePath "$GITHUB_WORKSPACE/build/VLC-iOS-Release.xcarchive" \
            clean archive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY=""

      - name: Create unsigned IPA from archive
        run: |
          set -euo pipefail
          APP_PATH="$(find "$GITHUB_WORKSPACE/build/VLC-iOS-Release.xcarchive/Products/Applications" -maxdepth 1 -type d -name "*.app" | head -n 1)"
          test -d "$APP_PATH"
          rm -rf "$GITHUB_WORKSPACE/build/Payload"
          mkdir -p "$GITHUB_WORKSPACE/build/Payload"
          cp -R "$APP_PATH" "$GITHUB_WORKSPACE/build/Payload/"
          cd "$GITHUB_WORKSPACE/build"
          zip -qry "VLC-iOS-Release-unsigned.ipa" "Payload"

      - name: Upload unsigned IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: VLC-iOS-Release-unsigned-ipa
          path: |
            build/VLC-iOS-Release-unsigned.ipa
            build/VLC-iOS-Release.xcarchive
          if-no-files-found: error
